import React from 'react';
import PropTypes from 'prop-types';

const ChecklistBar = function (props){

    return (
        <div>
            asd
        </div>
    )
};

ChecklistBar.propTypes = {

};

export default ChecklistBar;
